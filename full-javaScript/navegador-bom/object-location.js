
/** 
 * location.href -> la url nos lleva http
 * host -> nos devuelve solo el dominio y el puerto, el link
 * hostname -> devuelve solo el dominio 
 * pathname -> nos devuelve la url ////
 */

 /** Métodos location
  * reload() -> recarga la pagina
  */
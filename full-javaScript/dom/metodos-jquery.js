/** MÉTODOS DE JQUERY EN JS VANILLA  
 * before() -> hermano anterior
 * after() -> hermano siguiente
 * prepend() -> primer hijo
 * append() -> último hijo
 * child.replaceWidth() -> remplazar un hijo
*/

/** Clonar y eliminar elementos
 * cloneNode() -> clonar un elemento del dom
 * remove() -> 
 */

 /** Crear elementos
  * createElement() -> crea un  nuevo elemento 
  * createDocumentFragment() -> permite guardar elememntos en memoria, estructuras html antes de pintarlos, eso ahorra mucho en memoria, si se necesita  dibujar muchos elelemntos en el dom, lo mejor seria  utilizar createDocumentFragment()
  * en dado caso se puede utilizar las dos, pero si  se ejemplo recorrera un  arreglo con muchos elementos para dibujar en el dom lo mejor es usar fragment lo almacena en memoria y despues se añade con un  appendChild en el dom
  */